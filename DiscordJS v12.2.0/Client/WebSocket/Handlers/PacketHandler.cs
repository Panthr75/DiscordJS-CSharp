namespace DiscordJS
{
    class PacketHandler
    {
        /// <summary>
        /// Gets the packet handler for the specific type of packet
        /// </summary>
        /// <param name="type">The type of packet</param>
        /// <returns>The packet handler, or <see cref="null"/> if not found</returns>
        public static PacketHandler Get(string type)
        {
            //
        }
    }
}